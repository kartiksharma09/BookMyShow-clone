import {
    SEARCH_MOVIES,
    SEARCH_MOVIES_FAIL
} from '../actions/movies';


initialState = {
    movies: [],
    loading: true
};


const movies = (state = initialState, action) => {

}